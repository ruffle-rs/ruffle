package  {
	import flash.display.MovieClip
	
	public class OverButtonShape extends EventWatcher {
		public function OverButtonShape() {
			trace("//Constructed OverButtonShape (", this.name, "): this.parent = " + this.parent + " this.stage " + this.stage);
		}
	}
	
}

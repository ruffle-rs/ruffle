package {

import flash.display.*;

public class Test extends Sprite {
    public var dt;
    public var dt2;
    public var dt3;
    public var dt4;
    public var dt5;
    public var dt6;
    public var dt7;
    public var dt8;

    public function Test() {
        trace(dt.text);
        trace(dt2.text);
        trace(dt3.text);
        trace(dt4.text);
        trace(dt5.text);
        trace(dt6.text);
        trace(dt7.text);
        trace(dt8.text);
    }
}

}

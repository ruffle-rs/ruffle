package {
	import flash.utils.Dictionary;

	public class Helper {
		// The framescript order is flash-specific, so
		// we add messages here and sort them to avoid needing
		// to reproduce Flash's behavior
		public static var framescriptCounts: Object = new Object();
	}
}
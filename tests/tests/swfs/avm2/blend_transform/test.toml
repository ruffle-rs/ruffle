num_frames = 1
known_failure = true # Broken after the inaccuracy in getPixel/getPixel32 was fixed

[image_comparisons.output]
tolerance = 0

[player_options]
with_renderer = { optional = false, sample_count = 1 }

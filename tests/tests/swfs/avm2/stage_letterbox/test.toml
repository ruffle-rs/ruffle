num_frames = 3

# FIXME - add the ability to capture multiple frames,
# so we can test both vertical and horizontal letterboxing
[image_comparison]
tolerance = 0

[player_options]
with_renderer = { optional = false, sample_count = 1 }

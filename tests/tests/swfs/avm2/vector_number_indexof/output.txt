/// var a: Vector.<Number> = new <Number>[1,2,3,4];
/// var b: Vector.<Number> = new <Number>[5, NaN, -5, 0];
/// a.indexOf(0);
-1
/// a.indexOf(1);
0
/// a.indexOf(2);
1
/// b.indexOf(3);
-1
/// b.indexOf(-5);
2
/// b.indexOf(NaN);
-1
/// a.indexOf(0, 2);
-1
/// a.indexOf(1, 2);
-1
/// a.indexOf(2, 2);
-1
/// b.indexOf(3, 2);
-1
/// b.indexOf(-5, 2);
2
/// b.indexOf(NaN, 2);
-1
/// a.indexOf(0, -1);
-1
/// a.indexOf(1, -1);
-1
/// a.indexOf(2, -1);
-1
/// b.indexOf(3, -1);
-1
/// b.indexOf(-5, -1);
-1
/// b.indexOf(NaN, -1);
-1

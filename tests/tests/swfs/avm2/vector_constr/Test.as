package {
	public class Test {
	}
}

trace("///var a_bool = new Vector.<Boolean>(2);");
var a_bool = new Vector.<Boolean>(2);

trace("///var b_bool = new Vector.<Boolean>(3, true);");
var b_bool = new Vector.<Boolean>(3, true);

trace("///var c_bool = new Vector.<Boolean>();");
var c_bool = new Vector.<Boolean>();

trace("///a_bool.length");
trace(a_bool.length);

trace("///a_bool.fixed");
trace(a_bool.fixed);

trace("///b_bool.length");
trace(b_bool.length);

trace("///b_bool.fixed");
trace(b_bool.fixed);

trace("///c_bool.length");
trace(c_bool.length);

trace("///c_bool.fixed");
trace(c_bool.fixed);

class Superclass {
	
}

class Subclass extends Superclass {
	
}

trace("/// var a0_class = new Superclass();");
var a0_class = new Superclass();

trace("/// var a1_class = new Subclass();");
var a1_class = new Subclass();

trace("///var a_class = new Vector.<Superclass>(2);");
var a_class = new Vector.<Superclass>(2);

trace("///var b_class = new Vector.<Superclass>(3, true);");
var b_class = new Vector.<Superclass>(3, true);

trace("///var c_class = new Vector.<Superclass>();");
var c_class = new Vector.<Superclass>();

trace("///a_class.length");
trace(a_class.length);

trace("///a_class.fixed");
trace(a_class.fixed);

trace("///b_class.length");
trace(b_class.length);

trace("///b_class.fixed");
trace(b_class.fixed);

trace("///c_class.length");
trace(c_class.length);

trace("///c_class.fixed");
trace(c_class.fixed);

trace("///var a_int = new Vector.<int>(2);");
var a_int = new Vector.<int>(2);

trace("///var b_int = new Vector.<int>(3, true);");
var b_int = new Vector.<int>(3, true);

trace("///var c_int = new Vector.<int>();");
var c_int = new Vector.<int>();

trace("///a_int.length");
trace(a_int.length);

trace("///a_int.fixed");
trace(a_int.fixed);

trace("///b_int.length");
trace(b_int.length);

trace("///b_int.fixed");
trace(b_int.fixed);

trace("///c_int.length");
trace(c_int.length);

trace("///c_int.fixed");
trace(c_int.fixed);

trace("///var a_number = new Vector.<Number>(2);");
var a_number = new Vector.<Number>(2);

trace("///var b_number = new Vector.<Number>(3, true);");
var b_number = new Vector.<Number>(3, true);

trace("///var c_number = new Vector.<Number>();");
var c_number = new Vector.<Number>();

trace("///a_number.length");
trace(a_number.length);

trace("///a_number.fixed");
trace(a_number.fixed);

trace("///b_number.length");
trace(b_number.length);

trace("///b_number.fixed");
trace(b_number.fixed);

trace("///c_number.length");
trace(c_number.length);

trace("///c_number.fixed");
trace(c_number.fixed);

trace("///var a_string = new Vector.<String>(2);");
var a_string = new Vector.<String>(2);

trace("///var b_string = new Vector.<String>(3, true);");
var b_string = new Vector.<String>(3, true);

trace("///var c_string = new Vector.<String>();");
var c_string = new Vector.<String>();

trace("///a_string.length");
trace(a_string.length);

trace("///a_string.fixed");
trace(a_string.fixed);

trace("///b_string.length");
trace(b_string.length);

trace("///b_string.fixed");
trace(b_string.fixed);

trace("///c_string.length");
trace(c_string.length);

trace("///c_string.fixed");
trace(c_string.fixed);

trace("///var a_uint = new Vector.<uint>(2);");
var a_uint = new Vector.<uint>(2);

trace("///var b_uint = new Vector.<uint>(3, true);");
var b_uint = new Vector.<uint>(3, true);

trace("///var c_uint = new Vector.<uint>();");
var c_uint = new Vector.<uint>();

trace("///a_uint.length");
trace(a_uint.length);

trace("///a_uint.fixed");
trace(a_uint.fixed);

trace("///b_uint.length");
trace(b_uint.length);

trace("///b_uint.fixed");
trace(b_uint.fixed);

trace("///c_uint.length");
trace(c_uint.length);

trace("///c_uint.fixed");
trace(c_uint.fixed);

trace("///var a_vector = new Vector.<Vector.<int>>(2);");
var a_vector = new Vector.<Vector.<int>>(2);

trace("///var b_vector = new Vector.<Vector.<int>>(3, true);");
var b_vector = new Vector.<Vector.<int>>(3, true);

trace("///var c_vector = new Vector.<Vector.<int>>();");
var c_vector = new Vector.<Vector.<int>>();

trace("///a_vector.length");
trace(a_vector.length);

trace("///a_vector.fixed");
trace(a_vector.fixed);

trace("///b_vector.length");
trace(b_vector.length);

trace("///b_vector.fixed");
trace(b_vector.fixed);

trace("///c_vector.length");
trace(c_vector.length);

trace("///c_vector.fixed");
trace(c_vector.fixed);
num_frames = 1


# FIXME: High max_outliers. FP renders objects with a matrix3D transformation in a different way from 2D objects especially around the object edges.
[[image_comparisons.output.checks]]
tolerance = 100
max_outliers = 2000

[[image_comparisons.output.checks]]
tolerance = 10
max_outliers = 5000

[player_options]
with_renderer = { optional = true, quality = "high" }

num_ticks = 1
# Ruffle's JPEG decoder produces slightly different results depending on the platform,
# and `[approximations]` can't be used for hexadecimal pixel values.
known_failure.ruffle_check = false

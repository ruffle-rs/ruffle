num_ticks = 1

# Antialiasing along the curved colour boundaries of the radial bands.
[image_comparisons.output]
tolerance = 1
max_outliers = 3000

[player_options]
with_renderer = { optional = false }

[[compilers]]
type = "Asc"
target = "test.swf"
class = "Test"
scripts = ["Test.as"]
swf_version = 43

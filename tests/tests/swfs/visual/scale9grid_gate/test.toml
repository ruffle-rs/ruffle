num_ticks = 1

# Antialiasing along the diagonal edges of the rotated and skewed rows.
[image_comparisons.output]
tolerance = 1
max_outliers = 2400

[player_options]
with_renderer = { optional = false }

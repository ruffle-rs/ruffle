num_ticks=60

# FIXME Disabled because there's no expected image,
#   also the generated output is blank :(
# [image_comparisons.output]
# tolerance = 1

[player_options]
with_renderer = { optional = true, quality = "low" }
with_video = true
